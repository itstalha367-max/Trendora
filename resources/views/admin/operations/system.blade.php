@extends('layouts.admin') @section('title','System Status') @section('page-title','System Status') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Technical health</span><h2>System status</h2><p>Runtime checks for the Laravel application, database, cache and public storage.</p></div></div><div class="ad-card-grid">@foreach($checks as $c)<div class="card p-4"><div class="d-flex justify-content-between align-items-start"><div class="ad-icon-square {{ $c['ok']?'is-ok':'is-warn' }}"><i class="fa-solid {{ $c['ok']?'fa-check':'fa-triangle-exclamation' }}"></i></div><span class="ad-status {{ $c['ok']?'is-active':'is-urgent' }}">{{ $c['ok']?'Healthy':'Attention' }}</span></div><h4 class="mt-4">{{ $c['name'] }}</h4><p class="text-muted mb-0">{{ $c['value'] }}</p></div>@endforeach</div><div class="card p-4 mt-4"><h5>Deployment notes</h5><p class="text-muted mb-0">For production keep APP_DEBUG disabled, configure queues/cron as required, ensure <code>php artisan storage:link</code> exists, and keep .env secrets outside public source control.</p></div>@endsection