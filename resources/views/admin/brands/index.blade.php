@extends('layouts.admin') @section('title','Brands') @section('page-title','Brands') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Catalog</span><h2>Brands</h2><p>Organize products by manufacturer and build stronger discovery.</p></div><a class="btn btn-primary" href="{{ route('admin.brands.create') }}"><i class="fa-solid fa-plus me-2"></i>Add brand</a></div><div class="card p-3"><div class="table-responsive"><table class="table"><thead><tr><th>Brand</th><th>Products</th><th>Featured</th><th>Status</th><th>Order</th><th></th></tr></thead><tbody>@forelse($brands as $b)<tr><td><div class="d-flex align-items-center gap-3">@if($b->logo)<img class="ad-thumb" src="{{ asset('storage/'.$b->logo) }}">@else<div class="ad-thumb ad-thumb-placeholder">{{ strtoupper(substr($b->name,0,1)) }}</div>@endif<div><strong>{{ $b->name }}</strong><small class="d-block text-muted">/{{ $b->slug }}</small></div></div></td><td>{{ $b->products_count }}</td><td>{{ $b->featured?'Yes':'No' }}</td><td><span class="ad-status {{ $b->status?'is-active':'is-inactive' }}">{{ $b->status?'Active':'Hidden' }}</span></td><td>{{ $b->sort_order }}</td><td class="text-end"><a class="btn btn-sm btn-outline-light" href="{{ route('admin.brands.edit',$b) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.brands.destroy',$b) }}" onsubmit="return confirm('Delete brand?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">No brands yet.</td></tr>@endforelse</tbody></table></div>{{ $brands->links() }}</div>@endsection