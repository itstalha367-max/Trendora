@extends('layouts.admin') @section('title','Purchase Orders') @section('page-title','Purchase Orders') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Procurement</span><h2>Purchase orders</h2><p>Order stock from suppliers into a specific warehouse and receive it with stock-movement audit.</p></div><a class="btn btn-primary" href="{{ route('admin.purchase-orders.create') }}">New purchase order</a></div><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>PO</th><th>Supplier</th><th>Warehouse</th><th>Items</th><th>Total</th><th>Status</th><th>Expected</th><th></th></tr></thead><tbody>@forelse($orders as $po)<tr><td><strong>{{ $po->po_number }}</strong></td><td>{{ $po->supplier?->name }}</td><td>{{ $po->warehouse?->name }}</td><td>{{ $po->items_count }}</td><td>{{ number_format($po->total,2) }}</td><td><span class="ad-status is-{{ $po->status }}">{{ $po->status }}</span></td><td>{{ $po->expected_at?->format('d M Y')?:'—' }}</td><td><a class="btn btn-sm btn-outline-light" href="{{ route('admin.purchase-orders.show',$po) }}">Open</a></td></tr>@empty<tr><td colspan="8" class="text-center text-muted py-5">No purchase orders.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $orders->links() }}</div>@endsection
