@extends('layouts.admin') @section('title','Product Questions') @section('page-title','Product Questions') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Pre-purchase support</span><h2>Product questions</h2><p>Answer customer questions and publish useful Q&A directly on product detail pages.</p></div><form><select class="form-select" name="status" onchange="this.form.submit()"><option value="">All</option>@foreach(['pending','published','hidden'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucfirst($s) }}</option>@endforeach</select></form></div><div class="row g-4">@forelse($questions as $q)<div class="col-xl-6"><div class="card p-4 h-100"><div class="d-flex justify-content-between"><div><small class="text-muted">{{ $q->product?->name }}</small><h5 class="mt-1">{{ $q->question }}</h5></div><span class="ad-status is-{{ $q->status }}">{{ $q->status }}</span></div><p class="text-muted small">Asked by {{ $q->user?->name?:$q->name?:'Guest' }} · {{ $q->created_at->diffForHumans() }}</p><form method="POST" action="{{ route('admin.questions.answer',$q) }}">@csrf @method('PUT')<textarea class="form-control" name="answer" rows="4" placeholder="Write a helpful answer" required>{{ $q->answer }}</textarea><div class="d-flex gap-2 mt-2"><select class="form-select" name="status"><option value="published" @selected($q->status==='published')>Publish</option><option value="hidden" @selected($q->status==='hidden')>Hide</option></select><button class="btn btn-primary">Save answer</button></div></form></div></div>@empty<div class="card p-5 text-center text-muted">No questions yet.</div>@endforelse</div><div class="mt-3">{{ $questions->links() }}</div>@endsection
