@extends('layouts.admin') @section('title','Banners') @section('page-title','Banner Studio') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Storefront creative</span><h2>Banner studio</h2><p>Control promotional creative across hero, category and checkout placements.</p></div><a href="{{ route('admin.banners.create') }}" class="btn btn-primary">+ New banner</a></div><div class="ad-card-grid">@forelse($banners as $b)<div class="card overflow-hidden">@if($b->image)<img class="ad-banner-preview" src="{{ asset('storage/'.$b->image) }}">@else<div class="ad-banner-empty"><i class="fa-regular fa-image"></i></div>@endif<div class="p-4"><div class="d-flex justify-content-between"><span class="ad-pill">{{ ucfirst($b->placement) }}</span><span class="ad-status {{ $b->status?'is-active':'is-inactive' }}">{{ $b->status?'Active':'Hidden' }}</span></div><h4 class="mt-3">{{ $b->title }}</h4><p class="text-muted">{{ $b->subtitle }}</p><a href="{{ route('admin.banners.edit',$b) }}" class="btn btn-sm btn-outline-light">Edit creative</a><form class="d-inline" method="POST" action="{{ route('admin.banners.destroy',$b) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></div></div>@empty<div class="card p-5 text-center text-muted">No banners created.</div>@endforelse</div><div class="mt-4">{{ $banners->links() }}</div>@endsection