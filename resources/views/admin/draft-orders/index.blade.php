@extends('layouts.admin')
@section('title','Draft Orders') @section('page-title','Draft Orders')
@section('content')
<div class="ad-page-head"><div><span class="ad-eyebrow">Sales workspace</span><h2>Draft orders</h2><p>Create quotes and staff-assisted orders before converting them into live fulfilment.</p></div><a class="btn btn-primary" href="{{ route('admin.draft-orders.create') }}"><i class="fa-solid fa-plus me-2"></i>New draft</a></div>
<div class="ad-stat-grid mb-4"><div class="ad-stat"><span>Draft</span><strong>{{ $stats['draft'] }}</strong><i class="fa-regular fa-file"></i></div><div class="ad-stat"><span>Sent</span><strong>{{ $stats['sent'] }}</strong><i class="fa-regular fa-paper-plane"></i></div><div class="ad-stat"><span>Converted</span><strong>{{ $stats['converted'] }}</strong><i class="fa-solid fa-check"></i></div><div class="ad-stat"><span>Open value</span><strong>{{ App\Models\Setting::get('currency_symbol','Rs') }} {{ number_format($stats['value'],2) }}</strong><i class="fa-solid fa-coins"></i></div></div>
<form class="ad-filterbar mb-3" method="GET"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Draft #, customer or email"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['draft','sent','converted','cancelled'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucfirst($s) }}</option>@endforeach</select><span></span><button class="btn btn-outline-light">Filter</button></form>
<div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Draft</th><th>Customer</th><th>Items</th><th>Total</th><th>Status</th><th>Updated</th><th></th></tr></thead><tbody>@forelse($drafts as $draft)<tr><td><strong>{{ $draft->draft_number }}</strong></td><td>{{ $draft->customer_name }}<small class="d-block text-muted">{{ $draft->customer_email }}</small></td><td>{{ $draft->items_count }}</td><td>{{ $draft->currency }} {{ number_format($draft->total,2) }}</td><td><span class="ad-status is-{{ $draft->status }}">{{ $draft->status }}</span></td><td>{{ $draft->updated_at->diffForHumans() }}</td><td class="text-end"><a class="btn btn-sm btn-outline-light" href="{{ route('admin.draft-orders.show',$draft) }}">Open</a></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">No draft orders yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $drafts->links() }}</div>
@endsection
