@extends('layouts.admin') @section('title','Email Templates') @section('page-title','Email Templates') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Messaging studio</span><h2>Email templates</h2><p>Edit transactional copy without touching mailer code. Variables use double-brace tokens.</p></div></div><div class="row g-4">@foreach($templates as $t)<div class="col-xl-6"><form class="card p-4 h-100" method="POST" action="{{ route('admin.templates.emails.update',$t) }}">@csrf @method('PUT')<div class="d-flex justify-content-between"><div><small class="text-muted">{{ $t->key }}</small><h5>{{ $t->name }}</h5></div><label class="form-check"><input class="form-check-input" type="checkbox" name="status" value="1" @checked($t->status)> Active</label></div><label class="form-label mt-3">Name</label><input class="form-control" name="name" value="{{ $t->name }}" required><label class="form-label mt-3">Subject</label><input class="form-control" name="subject" value="{{ $t->subject }}" required><label class="form-label mt-3">Body</label><textarea class="form-control" name="content" rows="7" required>{{ $t->content }}</textarea><small class="text-muted mt-2">Variables: {{ collect($t->variables??[])->join(', ') }}</small><button class="btn btn-primary mt-3">Save template</button></form></div>@endforeach</div>@endsection
