@extends('layouts.admin') @section('title','Collections') @section('page-title','Collections') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Merchandising</span><h2>Collections</h2><p>Curate seasonal, featured and campaign-ready product groups.</p></div><a class="btn btn-primary" href="{{ route('admin.collections.create') }}">+ New collection</a></div><div class="ad-card-grid">@forelse($collections as $c)<div class="card ad-collection-card">@if($c->image)<img src="{{ asset('storage/'.$c->image) }}">@endif<div class="p-4"><div class="d-flex justify-content-between"><span class="ad-pill">{{ $c->products_count }} products</span><span class="ad-status {{ $c->status?'is-active':'is-inactive' }}">{{ $c->status?'Live':'Hidden' }}</span></div><h4 class="mt-3">{{ $c->name }}</h4><p class="text-muted">{{ Str::limit($c->description,100) }}</p><a href="{{ route('admin.collections.edit',$c) }}" class="btn btn-sm btn-outline-light">Manage</a><form class="d-inline" method="POST" action="{{ route('admin.collections.destroy',$c) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Delete collection?')">Delete</button></form></div></div>@empty<div class="card p-5 text-center text-muted">No collections yet.</div>@endforelse</div><div class="mt-4">{{ $collections->links() }}</div>@endsection