@extends('layouts.admin') @section('title','Contact Inbox') @section('page-title','Contact Inbox') @section('content')
<div class="ad-page-head"><div><span class="ad-eyebrow">Direct enquiries</span><h2>Contact inbox</h2><p>Messages submitted through the storefront contact page.</p></div></div><div class="card p-3"><form class="ad-filterbar mb-3"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Name, email, subject"><select class="form-select" name="status"><option value="">All status</option>@foreach(['new','read','replied','closed'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucfirst($s) }}</option>@endforeach</select><button class="btn btn-primary">Filter</button></form><div class="table-responsive"><table class="table"><thead><tr><th>From</th><th>Subject</th><th>Status</th><th>Received</th><th></th></tr></thead><tbody>@forelse($contacts as $c)<tr><td><strong>{{ $c->name }}</strong><small class="d-block text-muted">{{ $c->email }}</small></td><td>{{ $c->subject }}</td><td><span class="ad-status is-{{ $c->status }}">{{ ucfirst($c->status) }}</span></td><td>{{ $c->created_at->diffForHumans() }}</td><td><a href="{{ route('admin.contacts.show',$c) }}" class="btn btn-sm btn-outline-light">Read</a></td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-muted">No messages.</td></tr>@endforelse</tbody></table></div>{{ $contacts->links() }}</div>@endsection