@extends('layouts.admin') @section('title','Campaigns') @section('page-title','Campaigns') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Growth studio</span><h2>Campaigns</h2><p>Plan promotional initiatives and monitor spend and conversion signals.</p></div><a class="btn btn-primary" href="{{ route('admin.campaigns.create') }}">+ New campaign</a></div><div class="ad-stat-grid mb-4"><div class="ad-stat"><span>Active</span><strong>{{ $stats['active'] }}</strong><i class="fa-solid fa-bolt"></i></div><div class="ad-stat"><span>Budget</span><strong>Rs {{ number_format($stats['budget'],0) }}</strong><i class="fa-solid fa-wallet"></i></div><div class="ad-stat"><span>Spent</span><strong>Rs {{ number_format($stats['spent'],0) }}</strong><i class="fa-solid fa-arrow-trend-up"></i></div><div class="ad-stat"><span>Conversions</span><strong>{{ number_format($stats['conversions']) }}</strong><i class="fa-solid fa-bullseye"></i></div></div><div class="card p-3"><div class="table-responsive"><table class="table"><thead><tr><th>Campaign</th><th>Type</th><th>Status</th><th>Schedule</th><th>Spend</th><th>CTR</th><th>CVR</th><th></th></tr></thead><tbody>@forelse($campaigns as $c)<tr><td><strong>{{ $c->name }}</strong></td><td>{{ ucfirst($c->type) }}</td><td><span class="ad-status is-{{ $c->status }}">{{ ucfirst($c->status) }}</span></td><td>{{ optional($c->starts_at)->format('d M') ?? '—' }} → {{ optional($c->ends_at)->format('d M') ?? '—' }}</td><td>Rs {{ number_format($c->spent,0) }} / {{ number_format($c->budget,0) }}</td><td>{{ $c->ctr }}%</td><td>{{ $c->conversion_rate }}%</td><td><a href="{{ route('admin.campaigns.edit',$c) }}" class="btn btn-sm btn-outline-light">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.campaigns.destroy',$c) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="8" class="text-center py-5 text-muted">No campaigns yet.</td></tr>@endforelse</tbody></table></div>{{ $campaigns->links() }}</div>@endsection