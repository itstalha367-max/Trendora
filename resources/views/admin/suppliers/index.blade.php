@extends('layouts.admin') @section('title','Suppliers') @section('page-title','Suppliers') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Procurement</span><h2>Suppliers</h2><p>Maintain sourcing contacts, lead times and product associations.</p></div><a href="{{ route('admin.suppliers.create') }}" class="btn btn-primary">+ Add supplier</a></div><div class="card p-3"><div class="table-responsive"><table class="table"><thead><tr><th>Supplier</th><th>Contact</th><th>Products</th><th>Lead time</th><th>Status</th><th></th></tr></thead><tbody>@forelse($suppliers as $s)<tr><td><strong>{{ $s->name }}</strong><small class="d-block text-muted">{{ $s->city }}, {{ $s->country }}</small></td><td>{{ $s->contact_name }}<small class="d-block text-muted">{{ $s->email }}</small></td><td>{{ $s->products_count }}</td><td>{{ $s->lead_time_days }} days</td><td><span class="ad-status is-{{ $s->status }}">{{ ucfirst($s->status) }}</span></td><td><a class="btn btn-sm btn-outline-light" href="{{ route('admin.suppliers.edit',$s) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.suppliers.destroy',$s) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">No suppliers yet.</td></tr>@endforelse</tbody></table></div>{{ $suppliers->links() }}</div>@endsection