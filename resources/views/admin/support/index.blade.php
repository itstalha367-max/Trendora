@extends('layouts.admin') @section('title','Support Inbox') @section('page-title','Support Inbox') @section('content')
<div class="ad-page-head"><div><span class="ad-eyebrow">Customer care</span><h2>Support inbox</h2><p>Prioritize conversations and keep every customer issue moving.</p></div></div><div class="ad-stat-grid mb-4"><div class="ad-stat"><span>Open</span><strong>{{ $stats['open'] }}</strong><i class="fa-regular fa-envelope-open"></i></div><div class="ad-stat"><span>In progress</span><strong>{{ $stats['progress'] }}</strong><i class="fa-solid fa-spinner"></i></div><div class="ad-stat"><span>Waiting</span><strong>{{ $stats['waiting'] }}</strong><i class="fa-regular fa-hourglass"></i></div><div class="ad-stat"><span>Urgent</span><strong>{{ $stats['urgent'] }}</strong><i class="fa-solid fa-triangle-exclamation"></i></div></div>
<div class="card p-3"><form class="ad-filterbar mb-3"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Ticket # or subject"><select name="status" class="form-select"><option value="">All status</option>@foreach(['open','in_progress','waiting_customer','resolved','closed'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ str_replace('_',' ',ucfirst($s)) }}</option>@endforeach</select><select name="priority" class="form-select"><option value="">All priority</option>@foreach(['low','normal','high','urgent'] as $p)<option value="{{ $p }}" @selected(request('priority')===$p)>{{ ucfirst($p) }}</option>@endforeach</select><button class="btn btn-primary">Filter</button></form><div class="table-responsive"><table class="table"><thead><tr><th>Ticket</th><th>Customer</th><th>Category</th><th>Priority</th><th>Status</th><th>Messages</th><th></th></tr></thead><tbody>@forelse($tickets as $t)<tr><td><strong>{{ $t->ticket_number }}</strong><small class="d-block text-muted">{{ $t->subject }}</small></td><td>{{ $t->user?->name }}</td><td>{{ ucfirst($t->category) }}</td><td><span class="ad-status is-{{ $t->priority }}">{{ ucfirst($t->priority) }}</span></td><td>{{ str_replace('_',' ',ucfirst($t->status)) }}</td><td>{{ $t->messages_count }}</td><td><a href="{{ route('admin.support.show',$t) }}" class="btn btn-sm btn-outline-light">Open</a></td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-muted">Inbox is clear.</td></tr>@endforelse</tbody></table></div>{{ $tickets->links() }}</div>@endsection