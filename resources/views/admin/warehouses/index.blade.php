@extends('layouts.admin') @section('title','Warehouses') @section('page-title','Warehouses') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Fulfilment network</span><h2>Warehouses</h2><p>Manage stock locations, contact details and default fulfilment hub.</p></div><a class="btn btn-primary" href="{{ route('admin.warehouses.create') }}">+ Add warehouse</a></div><div class="ad-card-grid">@forelse($warehouses as $w)<div class="card p-4"><div class="d-flex justify-content-between"><div class="ad-icon-square"><i class="fa-solid fa-warehouse"></i></div>@if($w->is_default)<span class="ad-pill">Default</span>@endif</div><h4 class="mt-3">{{ $w->name }}</h4><p class="text-muted mb-2">{{ $w->code }} · {{ $w->city }}, {{ $w->country }}</p><div class="ad-mini-metric"><span>Inventory records</span><strong>{{ $w->inventories_count }}</strong></div><div class="mt-3"><a href="{{ route('admin.warehouses.edit',$w) }}" class="btn btn-sm btn-outline-light">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.warehouses.destroy',$w) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></div></div>@empty<div class="card p-5 text-center text-muted">Create your first warehouse.</div>@endforelse</div><div class="mt-4">{{ $warehouses->links() }}</div>@endsection