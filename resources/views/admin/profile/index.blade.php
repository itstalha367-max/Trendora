@extends('layouts.admin') @section('title','Admin Profile') @section('page-title','Admin Profile') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Your account</span><h2>Admin profile & security</h2><p>Manage your administrator identity, password and two-factor authentication.</p></div></div><div class="row g-4"><div class="col-lg-7"><form class="card p-4" method="POST" action="{{ route('admin.profile.update') }}">@csrf @method('PUT')<h5>Profile</h5><div class="row g-3"><div class="col-md-6"><label class="form-label">Name</label><input class="form-control" name="name" value="{{ $user->name }}" required></div><div class="col-md-6"><label class="form-label">Email</label><input class="form-control" type="email" name="email" value="{{ $user->email }}" required></div><div class="col-12"><label class="form-label">Phone</label><input class="form-control" name="phone" value="{{ $user->phone }}"></div></div><button class="btn btn-primary mt-3">Save profile</button></form><form class="card p-4 mt-4" method="POST" action="{{ route('admin.profile.password') }}">@csrf @method('PUT')<h5>Change password</h5><label class="form-label">Current password</label><input class="form-control" type="password" name="current_password" required><label class="form-label mt-3">New password</label><input class="form-control" type="password" name="password" required><label class="form-label mt-3">Confirm password</label><input class="form-control" type="password" name="password_confirmation" required><button class="btn btn-outline-light mt-3">Update password</button></form></div><div class="col-lg-5"><div class="card p-4 ad-sticky-card"><div class="ad-icon-square is-ok"><i class="fa-solid fa-shield"></i></div><h5 class="mt-3">Two-factor authentication</h5><p class="text-muted">{{ $user->google2fa_secret?'2FA is enabled on this administrator account.':'Protect privileged access with authenticator-based 2FA.' }}</p><a class="btn btn-primary" href="{{ route('2fa.setup') }}">{{ $user->google2fa_secret?'Manage 2FA':'Enable 2FA' }}</a></div></div></div>@endsection
