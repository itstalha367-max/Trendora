@extends('layouts.admin')
@section('title',$page->exists?'Edit CMS Page':'Create CMS Page') @section('page-title','CMS Editor')
@section('content')
<div class="ad-page-head"><div><span class="ad-eyebrow">Content studio</span><h2>{{ $page->exists?'Edit page':'Create page' }}</h2><p>Publish branded information pages without editing Blade files.</p></div><a class="btn btn-outline-light" href="{{ route('admin.cms.index') }}">Back</a></div>
<form class="row g-4" method="POST" action="{{ $page->exists ? route('admin.cms.update',$page) : route('admin.cms.store') }}">@csrf @if($page->exists) @method('PUT') @endif
<div class="col-xl-8"><div class="card p-4"><label class="form-label">Title</label><input class="form-control" name="title" value="{{ old('title',$page->title) }}" required><div class="row g-3 mt-1"><div class="col-md-7"><label class="form-label">Slug</label><input class="form-control" name="slug" value="{{ old('slug',$page->slug) }}" placeholder="auto-generated-from-title"></div><div class="col-md-5"><label class="form-label">Eyebrow</label><input class="form-control" name="eyebrow" value="{{ old('eyebrow',$page->eyebrow) }}"></div></div><label class="form-label mt-3">Excerpt</label><textarea class="form-control" rows="3" name="excerpt">{{ old('excerpt',$page->excerpt) }}</textarea><label class="form-label mt-3">Content</label><textarea class="form-control" rows="16" name="content" placeholder="HTML is supported for trusted admin content.">{{ old('content',$page->content) }}</textarea></div></div>
<div class="col-xl-4"><div class="card p-4 mb-4"><h5>Publishing</h5><label class="form-check mt-3"><input class="form-check-input" type="checkbox" name="status" value="1" @checked(old('status',$page->exists?$page->status:true))> Published</label><label class="form-label mt-3">Sort order</label><input class="form-control" type="number" min="0" name="sort_order" value="{{ old('sort_order',$page->sort_order??0) }}"><button class="btn btn-primary w-100 mt-4">Save page</button></div><div class="card p-4"><h5>SEO</h5><label class="form-label mt-3">Meta title</label><input class="form-control" name="meta_title" value="{{ old('meta_title',$page->meta_title) }}"><label class="form-label mt-3">Meta description</label><textarea class="form-control" rows="5" name="meta_description">{{ old('meta_description',$page->meta_description) }}</textarea></div></div></form>
@endsection
