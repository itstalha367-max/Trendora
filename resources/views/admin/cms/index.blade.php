@extends('layouts.admin')
@section('title','CMS Pages') @section('page-title','CMS Pages')
@section('content')
<div class="ad-page-head"><div><span class="ad-eyebrow">Store content</span><h2>Custom pages</h2><p>Create reusable storefront pages with SEO metadata.</p></div><a class="btn btn-primary" href="{{ route('admin.cms.create') }}"><i class="fa-solid fa-plus me-2"></i>New page</a></div>
<div class="card p-4"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Page</th><th>URL</th><th>Status</th><th>Order</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($pages as $page)<tr><td><strong>{{ $page->title }}</strong><small class="d-block text-muted">{{ Str::limit($page->excerpt,70) }}</small></td><td><a target="_blank" href="{{ route('cms.show',$page->slug) }}">/pages/{{ $page->slug }}</a></td><td><span class="ad-status {{ $page->status?'is-active':'is-inactive' }}">{{ $page->status?'Published':'Draft' }}</span></td><td>{{ $page->sort_order }}</td><td class="text-end"><a class="btn btn-sm btn-outline-light" href="{{ route('admin.cms.edit',$page) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.cms.destroy',$page) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Delete this page?')">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No CMS pages yet.</td></tr>@endforelse</tbody></table></div>{{ $pages->links() }}</div>
@endsection
