@extends('layouts.admin') @section('title','Navigation') @section('page-title','Navigation Builder') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Content architecture</span><h2>Navigation menus</h2><p>Control header and footer link groups from the admin panel.</p></div></div><div class="row g-4">@foreach($menus as $menu)<div class="col-xl-4"><div class="card p-4 h-100"><div class="d-flex justify-content-between"><div><small class="text-muted">{{ $menu->location }}</small><h5>{{ $menu->name }}</h5></div><span class="ad-status is-active">active</span></div>@foreach($menu->allItems as $item)<div class="ad-list-row"><div><strong>{{ $item->label }}</strong><small>{{ $item->url }} · {{ $item->target }}</small></div><form method="POST" action="{{ route('admin.navigation.items.destroy',$item) }}">@csrf @method('DELETE')<button class="ad-link-button">Remove</button></form></div>@endforeach<form class="mt-3" method="POST" action="{{ route('admin.navigation.items.store',$menu) }}">@csrf<input class="form-control mb-2" name="label" placeholder="Label" required><input class="form-control mb-2" name="url" placeholder="/products or https://..." required><div class="row g-2"><div class="col-5"><select class="form-select" name="target"><option value="_self">Same tab</option><option value="_blank">New tab</option></select></div><div class="col-3"><input class="form-control" type="number" name="sort_order" value="0" min="0"></div><div class="col-4"><label class="form-check mt-2"><input class="form-check-input" type="checkbox" name="status" value="1" checked> Active</label></div></div><button class="btn btn-outline-light w-100 mt-2">Add link</button></form></div></div>@endforeach</div>@endsection
