@extends('layouts.admin') @section('title','Stock History') @section('page-title','Stock movement history') @section('content')<div class="ad-page-head"><div><a class="ad-back" href="{{ route('admin.inventory.index') }}">← Inventory</a><h2>{{ $inventory->product?->name }}</h2><p>{{ $inventory->warehouse?->name }} · Current stock {{ $inventory->quantity }}</p></div></div><div class="card p-3"><div class="table-responsive"><table class="table"><thead><tr><th>Date</th><th>Type</th><th>Change</th><th>Before</th><th>After</th><th>Reference</th><th>By</th><th>Note</th></tr></thead><tbody>@forelse($movements as $m)<tr><td>{{ $m->created_at->format('d M Y H:i') }}</td><td>{{ ucfirst($m->type) }}</td><td><strong class="{{ $m->quantity>=0?'text-success':'text-danger' }}">{{ $m->quantity>=0?'+':'' }}{{ $m->quantity }}</strong></td><td>{{ $m->before_quantity }}</td><td>{{ $m->after_quantity }}</td><td>{{ $m->reference }}</td><td>{{ $m->user?->name }}</td><td>{{ $m->note }}</td></tr>@empty<tr><td colspan="8" class="text-center py-5 text-muted">No stock movements yet.</td></tr>@endforelse</tbody></table></div>{{ $movements->links() }}</div>@endsection