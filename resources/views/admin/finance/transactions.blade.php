@extends('layouts.admin') @section('title','Transactions') @section('page-title','Transactions') @section('content')
<div class="ad-page-head"><div><span class="ad-eyebrow">Payment ledger</span><h2>Transactions</h2><p>Search captures, charges, refunds and failed payment attempts across all configured gateways.</p></div><a class="btn btn-outline-light" href="{{ route('admin.finance.index') }}">Finance overview</a></div>
<div class="ad-stat-grid mb-4"><div class="ad-stat"><span>Captured</span><strong>{{ number_format($stats['captured'],2) }}</strong><i class="fa-solid fa-circle-check"></i></div><div class="ad-stat"><span>Refunded</span><strong>{{ number_format($stats['refunded'],2) }}</strong><i class="fa-solid fa-rotate-left"></i></div><div class="ad-stat"><span>Pending value</span><strong>{{ number_format($stats['pending'],2) }}</strong><i class="fa-solid fa-clock"></i></div><div class="ad-stat"><span>Failed attempts</span><strong>{{ number_format($stats['failed']) }}</strong><i class="fa-solid fa-triangle-exclamation"></i></div></div>
<div class="card p-4"><form class="row g-3 mb-4"><div class="col-md-4"><select class="form-select" name="gateway"><option value="">All gateways</option>@foreach($gateways as $g)<option value="{{ $g }}" @selected(request('gateway')===$g)>{{ ucfirst($g) }}</option>@endforeach</select></div><div class="col-md-3"><select class="form-select" name="type"><option value="">All types</option>@foreach(['charge','capture','refund'] as $t)<option value="{{ $t }}" @selected(request('type')===$t)>{{ ucfirst($t) }}</option>@endforeach</select></div><div class="col-md-3"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['pending','succeeded','failed'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucfirst($s) }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Date</th><th>Order</th><th>Gateway</th><th>Type</th><th>Amount</th><th>Status</th><th>Reference</th></tr></thead><tbody>@forelse($transactions as $tx)<tr><td>{{ $tx->created_at->format('d M Y H:i') }}</td><td>@if($tx->order)<a href="{{ route('admin.orders.show',$tx->order) }}">{{ $tx->order->order_number }}</a><small class="d-block text-muted">{{ $tx->order->user?->email }}</small>@else—@endif</td><td>{{ ucfirst($tx->gateway) }}</td><td>{{ ucfirst($tx->type) }}</td><td><strong>{{ $tx->currency }} {{ number_format($tx->amount,2) }}</strong></td><td><span class="ad-status {{ $tx->status==='succeeded'?'is-active':($tx->status==='failed'?'is-inactive':'') }}">{{ $tx->status }}</span></td><td><code>{{ $tx->transaction_id ?: '—' }}</code></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">No transactions match these filters.</td></tr>@endforelse</tbody></table></div>{{ $transactions->links() }}</div>
@endsection
