@extends('layouts.admin') @section('title','Domains') @section('page-title','Domains') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Storefront infrastructure</span><h2>Domains</h2><p>Track primary custom domain, DNS verification and SSL readiness. DNS/SSL provisioning remains host-provider dependent.</p></div><form class="d-flex gap-2" method="POST" action="{{ route('admin.domains.store') }}">@csrf<input class="form-control" name="domain" placeholder="shop.example.com" required><button class="btn btn-primary">Add</button></form></div><div class="ad-card-grid">@forelse($domains as $d)<div class="card p-4"><div class="d-flex justify-content-between"><div class="ad-icon-square"><i class="fa-solid fa-globe"></i></div>@if($d->primary)<span class="ad-status is-active">primary</span>@endif</div><h5 class="mt-3">{{ $d->domain }}</h5><div class="ad-mini-metric"><span>Verification</span><strong>{{ $d->verification_status }}</strong></div><div class="ad-mini-metric"><span>SSL</span><strong>{{ $d->ssl_status }}</strong></div><div class="d-flex gap-2 mt-3">@unless($d->primary)<form method="POST" action="{{ route('admin.domains.primary',$d) }}">@csrf<button class="btn btn-sm btn-outline-light">Make primary</button></form>@endunless<form method="POST" action="{{ route('admin.domains.verify',$d) }}">@csrf<button class="btn btn-sm btn-outline-light">Verify</button></form>@unless($d->primary)<form method="POST" action="{{ route('admin.domains.destroy',$d) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Remove</button></form>@endunless</div></div>@empty<div class="card p-5 text-center text-muted">No custom domains configured.</div>@endforelse</div>@endsection
