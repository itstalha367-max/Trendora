@extends('layouts.admin') @section('title','Integrations') @section('page-title','Integrations') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Connected commerce</span><h2>Integrations</h2><p>Manage encrypted credentials for analytics, marketing, shipping and operational connectors.</p></div></div><div class="ad-card-grid">@foreach($integrations as $i)<form class="card p-4" method="POST" action="{{ route('admin.integrations.update',$i) }}">@csrf @method('PUT')<div class="d-flex justify-content-between align-items-start"><div class="ad-icon-square"><i class="fa-solid fa-plug"></i></div><span class="ad-status {{ $i->enabled?'is-active':'is-inactive' }}">{{ $i->health_status }}</span></div><h5 class="mt-3">{{ $i->name }}</h5><small class="text-muted">{{ ucfirst(str_replace('-',' ',$i->category)) }} · secrets encrypted at rest</small><label class="form-check mt-3"><input class="form-check-input" type="checkbox" name="enabled" value="1" @checked($i->enabled)> Enabled</label><label class="form-label mt-3">API key / token</label><input class="form-control" type="password" name="api_key" placeholder="Leave blank to keep existing"><label class="form-label mt-3">API secret</label><input class="form-control" type="password" name="api_secret" placeholder="Leave blank to keep existing"><label class="form-label mt-3">Endpoint</label><input class="form-control" name="endpoint" value="{{ $i->config['endpoint']??'' }}" placeholder="https://..."><label class="form-label mt-3">Account / property ID</label><input class="form-control" name="account_id" value="{{ $i->config['account_id']??'' }}"><button class="btn btn-primary mt-3">Save integration</button></form>@endforeach</div>@endsection
