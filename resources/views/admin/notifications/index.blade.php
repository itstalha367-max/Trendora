@extends('layouts.admin') @section('title','Admin Notifications') @section('page-title','Admin Notifications') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Operations inbox</span><h2>Notifications</h2><p>Your administrator account's database notifications.</p></div><form method="POST" action="{{ route('admin.notifications.read') }}">@csrf<button class="btn btn-outline-light">Mark all read</button></form></div><div class="card p-4">@forelse($notifications as $n)<div class="ad-list-row"><div><strong>{{ $n->data['title']??'Notification' }}</strong><small>{{ $n->data['message']??'' }} · {{ $n->created_at->diffForHumans() }}</small></div><span class="ad-status {{ $n->read_at?'is-read':'is-pending' }}">{{ $n->read_at?'read':'new' }}</span></div>@empty<p class="text-muted text-center py-5">No admin notifications.</p>@endforelse</div><div class="mt-3">{{ $notifications->links() }}</div>@endsection
