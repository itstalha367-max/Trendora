@extends('layouts.admin')
@section('title','Returns & Refunds') @section('page-title','Returns & Refunds')
@section('content')
<div class="ad-page-head"><div><span class="ad-eyebrow">Reverse logistics</span><h2>Returns & refunds</h2><p>Review customer requests, approve returns and track refund completion.</p></div></div>
<div class="ad-stat-grid mb-4"><div class="ad-stat"><span>Pending</span><strong>{{ $stats['pending'] }}</strong><i class="fa-regular fa-clock"></i></div><div class="ad-stat"><span>Approved</span><strong>{{ $stats['approved'] }}</strong><i class="fa-solid fa-check"></i></div><div class="ad-stat"><span>Refunded</span><strong>{{ $stats['refunded'] }}</strong><i class="fa-solid fa-rotate-left"></i></div><div class="ad-stat"><span>Requested value</span><strong>Rs {{ number_format($stats['requested'],0) }}</strong><i class="fa-solid fa-wallet"></i></div></div>
<div class="card p-3"><form class="ad-filterbar mb-3"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Request #, customer..."><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['pending','approved','rejected','received','refunded','closed'] as $s)<option @selected(request('status')===$s)>{{ $s }}</option>@endforeach</select><select class="form-select" name="type"><option value="">All types</option><option value="return" @selected(request('type')==='return')>Return</option><option value="refund" @selected(request('type')==='refund')>Refund</option></select><button class="btn btn-primary">Filter</button></form>
<div class="table-responsive"><table class="table align-middle"><thead><tr><th>Request</th><th>Customer</th><th>Order</th><th>Type</th><th>Amount</th><th>Status</th><th></th></tr></thead><tbody>@forelse($returns as $r)<tr><td><strong>{{ $r->request_number }}</strong><small class="d-block text-muted">{{ $r->created_at->diffForHumans() }}</small></td><td>{{ $r->user?->name }}<small class="d-block text-muted">{{ $r->user?->email }}</small></td><td>{{ $r->order?->order_number }}</td><td><span class="ad-pill">{{ ucfirst($r->type) }}</span></td><td>Rs {{ number_format($r->requested_amount ?? 0,2) }}</td><td><span class="ad-status is-{{ $r->status }}">{{ str_replace('_',' ',ucfirst($r->status)) }}</span></td><td><a class="btn btn-sm btn-outline-light" href="{{ route('admin.returns.show',$r) }}">Review</a></td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-muted">No return requests found.</td></tr>@endforelse</tbody></table></div>{{ $returns->links() }}</div>
@endsection