@extends('layouts.admin') @section('title','Staff') @section('page-title','Staff & Access') @section('content')<div class="ad-page-head"><div><span class="ad-eyebrow">Organization</span><h2>Staff accounts</h2><p>Manage administrators and attach granular permission roles.</p></div><div><a class="btn btn-outline-light" href="{{ route('admin.roles.index') }}">Roles & permissions</a> <a class="btn btn-primary" href="{{ route('admin.staff.create') }}">+ Add staff</a></div></div><div class="card p-3"><div class="table-responsive"><table class="table"><thead><tr><th>Staff member</th><th>Role</th><th>Phone</th><th>Joined</th><th></th></tr></thead><tbody>@foreach($staff as $u)<tr><td><div class="d-flex align-items-center gap-3"><div class="ad-avatar">{{ strtoupper(substr($u->name,0,1)) }}</div><div><strong>{{ $u->name }}</strong><small class="d-block text-muted">{{ $u->email }}</small></div></div></td><td>{{ $u->adminRole?->name ?? 'Super Admin' }}</td><td>{{ $u->phone ?: '—' }}</td><td>{{ $u->created_at->format('d M Y') }}</td><td><a class="btn btn-sm btn-outline-light" href="{{ route('admin.staff.edit',$u) }}">Edit</a>@if($u->id!==auth()->id())<form class="d-inline" method="POST" action="{{ route('admin.staff.destroy',$u) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Remove</button></form>@endif</td></tr>@endforeach</tbody></table></div>{{ $staff->links() }}</div>@endsection