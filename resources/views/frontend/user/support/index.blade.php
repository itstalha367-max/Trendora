@extends('layouts.app')
@section('title','Support Tickets — Trendora')
@section('content')<section class="tr-page"><div class="tr-shell"><div class="tr-page-head"><div><span class="tr-eyebrow">Support center</span><h1>Your support tickets</h1><p>One place for order, payment, return and account help.</p></div><a class="btn btn-primary" href="{{ route('support.create') }}"><i class="fa-solid fa-plus me-2"></i>New ticket</a></div><div class="tr-account-grid">@include('frontend.partials.account-nav')<div class="tr-stack">@forelse($tickets as $ticket)<a class="tr-card tr-list-card" href="{{ route('support.show',$ticket) }}"><div><div class="d-flex gap-2"><span class="tr-chip">{{ $ticket->ticket_number }}</span><span class="tr-chip">{{ ucfirst($ticket->category) }}</span></div><h5 class="mt-2 mb-1">{{ $ticket->subject }}</h5><small class="text-muted">Last activity {{ optional($ticket->last_reply_at)->diffForHumans() ?: $ticket->updated_at->diffForHumans() }}</small></div><div class="text-end"><span class="tr-status {{ $ticket->status }}">{{ str_replace('_',' ',$ticket->status) }}</span><small class="d-block text-muted mt-2">{{ ucfirst($ticket->priority) }} priority</small></div></a>@empty<div class="tr-empty tr-card"><i class="fa-regular fa-comments"></i><h4>No support tickets</h4><p>Create a ticket whenever you need help.</p></div>@endforelse<div>{{ $tickets->links() }}</div></div></div></div></section>@endsection
