@extends('layouts.app')
@section('title','My Reviews — Trendora')
@section('content')<section class="tr-page"><div class="tr-shell"><div class="tr-page-head"><div><span class="tr-eyebrow">Account</span><h1>My reviews</h1><p>See the product feedback you have submitted.</p></div></div><div class="tr-account-grid">@include('frontend.partials.account-nav')<div class="tr-stack">@forelse($reviews as $review)<div class="tr-card tr-list-card"><div class="d-flex align-items-start gap-3"><img class="tr-mini-product" src="{{ $review->product?->thumbnail_url ?? asset('images/no-image.png') }}" alt=""><div><span class="tr-stars">{{ str_repeat('★',$review->rating) }}{{ str_repeat('☆',5-$review->rating) }}</span><h5 class="mt-1 mb-1">{{ $review->product?->name ?? 'Product unavailable' }}</h5><p class="text-muted mb-0">{{ $review->comment ?: 'Rating submitted without a written comment.' }}</p></div></div><div class="text-end"><span class="tr-status {{ $review->status }}">{{ $review->status }}</span><small class="d-block text-muted mt-2">{{ $review->created_at->format('d M Y') }}</small></div></div>@empty<div class="tr-empty tr-card"><i class="fa-regular fa-star"></i><h4>No reviews yet</h4><p>Your submitted product feedback will show here.</p></div>@endforelse<div>{{ $reviews->links() }}</div></div></div></div></section>@endsection
