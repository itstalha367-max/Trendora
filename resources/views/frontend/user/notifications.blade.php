@extends('layouts.app')
@section('title','Notifications — Trendora')
@section('content')<section class="tr-page"><div class="tr-shell"><div class="tr-page-head"><div><span class="tr-eyebrow">Account</span><h1>Notifications</h1><p>Order, payment, account and support updates in one place.</p></div>@if(auth()->user()->unreadNotifications()->exists())<form method="POST" action="{{ route('user.notifications.read') }}">@csrf<button class="btn btn-outline-light">Mark all as read</button></form>@endif</div><div class="tr-account-grid">@include('frontend.partials.account-nav')<div class="tr-stack">@forelse($notifications as $notification)<div class="tr-card tr-notification {{ $notification->read_at ? '' : 'is-unread' }}"><div class="tr-icon-orb"><i class="fa-regular fa-bell"></i></div><div><h6>{{ data_get($notification->data,'title','Trendora update') }}</h6><p class="mb-1 text-muted">{{ data_get($notification->data,'message','You have a new account update.') }}</p><small class="text-muted">{{ $notification->created_at->diffForHumans() }}</small></div></div>@empty<div class="tr-empty tr-card"><i class="fa-regular fa-bell-slash"></i><h4>You’re all caught up</h4><p>New order and account updates will appear here.</p></div>@endforelse<div>{{ $notifications->links() }}</div></div></div></div></section>@endsection
