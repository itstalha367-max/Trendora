@extends('layouts.app') @section('title','Wallet & Store Credit') @section('content')<section class="tr-account-hero"><div class="tr-shell"><span class="tr-eyebrow">Account · Wallet</span><h1>Store credit</h1><p>Redeem Trendora gift cards and review your credit ledger.</p></div></section><section class="tr-section"><div class="tr-shell"><div class="row g-4"><div class="col-lg-4"><div class="tr-card p-4 tr-sticky"><span class="tr-eyebrow">Available balance</span><h2 class="display-6 fw-bold mt-2">{{ App\Models\Setting::get('currency_symbol','Rs') }} {{ number_format(auth()->user()->store_credit,2) }}</h2><p class="text-muted">Store credit is held on your account and can be used by supported checkout/payment flows.</p><form method="POST" action="{{ route('user.wallet.redeem') }}">@csrf<label class="form-label">Redeem gift card</label><input class="form-control" name="code" placeholder="TR-XXXX-XXXXXX" required><button class="btn btn-primary w-100 mt-2">Redeem to wallet</button></form></div></div><div class="col-lg-8"><div class="tr-card p-4"><div class="tr-section-title"><div><span>Ledger</span><h3>Credit activity</h3></div><i class="fa-solid fa-wallet"></i></div>@forelse($transactions as $t)<div class="d-flex justify-content-between align-items-center py-3 border-bottom border-secondary"><div><strong>{{ ucfirst(str_replace('_',' ',$t->type)) }}</strong><small class="d-block text-muted">{{ $t->note }} · {{ $t->created_at->format('d M Y, h:i A') }}</small></div><div class="text-end"><strong class="{{ $t->amount>=0?'text-success':'text-danger' }}">{{ $t->amount>=0?'+':'' }}{{ number_format($t->amount,2) }}</strong><small class="d-block text-muted">Balance {{ number_format($t->balance_after,2) }}</small></div></div>@empty<p class="text-muted py-5 text-center">No wallet activity yet.</p>@endforelse</div><div class="mt-3">{{ $transactions->links() }}</div></div></div></div></section>@endsection
